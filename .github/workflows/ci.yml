name: CI pipeline

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches: [ main ]

jobs:
  checks:
    name: CI
    runs-on: ubuntu-latest
    env:
      DJANGO_SETTINGS_MODULE: main.django.test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12.0"

      - name: Install uv
        uses: astral-sh/setup-uv@v1
        with:
          version: "0.7.13"

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Run Ruff
        run: uv run ruff check

      - name: Run Mypy
        run: uv run mypy
        env:
          PYTHONPATH: src/

      - name: Run Tests
        run: uv run src/manage.py test src/
